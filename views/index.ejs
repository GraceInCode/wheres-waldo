<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wheres Waldo</title>
    <style>
        #photo { max-width: 100%; cursor: crosshair; position: relative; }
        #markers { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        #target-box { position: absolute; display: none; border: 2px solid red; width: 50px; height: 50px; }
        #target-box select { width: 100%; }
        #timer { position: absolute; top: 10px; right: 10px; font-size: 10px; }
    </style>
</head>
<body>
    <div id="timer">Time: 0s</div>
    <img id="photo" src="<%= imageUrl %>" alt="Find them!">
    <div id="target-box">
        <select id="char-select">
            <% characters.forEach(char => { %>
            <option value="<%= char.id %>"><%= char.name %></option>
            <% }); %>
        </select>
    </div>
    <div id="markers"></div> <!-- For correct markers -->
    <script src="/script.js"></script> <!-- Frontend JS below -->
</body>
</html>